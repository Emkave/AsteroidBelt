#include "../Headers/Registry.h"

namespace Registry {
    RenderWindow * window = nullptr;
    constinit const unsigned short WIN_HEIGHT = 200;
    constinit const unsigned short WIN_LENGHT = 300;
    constinit const unsigned short FPS = 100;
}